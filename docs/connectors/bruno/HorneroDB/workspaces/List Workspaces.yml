info:
  name: List Workspaces
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/workspaces"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080/api/v1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/workspaces"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
