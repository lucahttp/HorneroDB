info:
  name: Unsubscribe Webhook
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/workspaces/:workspace_id/webhooks/:webhook_id"
  params:
    - name: workspace_id
      value: ""
      type: path
    - name: webhook_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/workspaces/:workspace_id/webhooks/:webhook_id"
      method: DELETE
      params:
        - name: workspace_id
          value: ""
          type: path
        - name: webhook_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
