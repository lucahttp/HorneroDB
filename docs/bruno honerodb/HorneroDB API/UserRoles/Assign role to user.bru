meta {
  name: Assign role to user
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/workspaces/:workspace_id/users/:user_id/role
  body: json
  auth: inherit
}

body:json {
  {
    "role_id": ""
  }
}
